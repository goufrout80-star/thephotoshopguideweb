# Copy this file to .env (or use these values as-is — see note below).

# --- Supabase (contact form) ---
# This points at the `page_contacts` table on JUST WHY US's shared admin
# dashboard project, not a project owned by this codebase. The anon key
# below is restricted server-side (RLS) to insert-only on that one table,
# so it's safe to commit/ship as-is — it is not a secret.
VITE_SUPABASE_URL=https://eawftyzjuwccwuxdszxf.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVhd2Z0eXpqdXdjY3d1eGRzenhmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI4NTY2NTcsImV4cCI6MjA5ODQzMjY1N30.AyO5dfrXNB1NVK1TDJCwoifS82w8YKeREUMczLWpFY0

# --- Analytics (optional) ---
# GA4 measurement ID, e.g. G-XXXXXXXXXX. Leave blank to disable analytics entirely.
VITE_GA_MEASUREMENT_ID=
